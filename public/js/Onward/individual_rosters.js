function Start(db) {
    buildpregame(
        db, 
        previousMatches = false, 
        teamStats = false, 
        roster = true, 
        live = true, 
        get_team_ranking = false, 
        game = 'onward');
}
